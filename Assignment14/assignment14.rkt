(display "original way with let*")
(newline)

(let* ((x 9) (y (- 4 11)) (z (+ x y)) (w (* z x)))
  (write x)
  (newline)
  (write y)
  (newline)
  (write z)
  (newline)
  (write w)
  (newline)

)

(newline)
(display "new way with just let")
(newline)

(let ((x 9) (y (- 4 11)))
  (let ((z (+ x y)))
    (let ((w (* z x)))
      (write x)
      (newline)
      (write y)
      (newline)
      (write z)
      (newline)
      (write w)
      (newline)
      )
    )
  )
